version: '3.8'

services:
  subscription-service:
    build: .
    container_name: subscription-service
    ports:
      - "8083:8083"
    depends_on:
      - subscriptiondb
    environment:
      SPRING_PROFILES_ACTIVE: default
    networks:
      - aforo-network

  subscriptiondb:
    image: postgres:14
    container_name: subscriptiondb
    environment:
      POSTGRES_DB: subscriptiondb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5440:5432" # External:Internal (optional)
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - aforo-network

networks:
  aforo-network:

volumes:
  pgdata:
