# Allow local connections inside the container
local   all             all                                     trust

# Allow connections from Docker network and host (macOS host appears as 192.168.65.1)
# For development only. Prefer scram-sha-256 auth in teams; here we use md5 for broad compatibility.
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5
