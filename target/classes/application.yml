server:
  port: 8083

spring:
  application:
    name: subscription-service
  datasource:
    url: jdbc:postgresql://localhost:5440/subscriptiondb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update  # ✅ enables auto table creation in dev
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml

product:
  service:
    base-url: http://54.238.204.246:8080  # Replace with actual URL if needed



logging:
  level:
    org.springframework.web.reactive.function.client: DEBUG
